import { jss, createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  gholi: {
    color: "red",
  },
  tee: {
    background: "green",
  },
  width: {
    width: 200,
  },
});
