import { createUseStyles } from "react-jss";

export default createUseStyles({
  tourContainer: {
    width: 200,
    height: 50,
    position: "absolute",
    border: "1px solid #222",
    background: "#ccc",
  },
});
