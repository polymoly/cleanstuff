import { createUseStyles } from "react-jss";

export default createUseStyles({
  item: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    width: "100%",
    padding: [16, 8],
    background: "#888",
  },
});
